//#include "Image.h"
#include <iostream>
#include <string>
//#include <random>
#include "encpass.h"

int main()
{
	//Image image = Image();
	//image.encryptImage("..\\pizza.jpg");



	/*FILE* image;

	errno_t image_byte = fopen_s(&image, "..\\pizza.jpg", "rb");
	fseek(image, 0, SEEK_END);
	int length = ftell(image);
	rewind(image);
	char* file_data = (char*)malloc((length + 1) * sizeof(char));
	fread(file_data, length, 1, image);
	std::cout << file_data;*/

	return 1;
}